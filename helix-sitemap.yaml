version: 1

sitemaps:
  website:
    origin: https://rockstar.adobeevents.com
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /query-index.json
        destination: /sitemap-en.xml
        hreflang: en
        alternate: /{path}
